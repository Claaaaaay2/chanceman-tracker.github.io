{
  "name": "chanceman-tracker",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "validate:data": "node scripts/validate-data.mjs",
    "check:item-mapping": "node scripts/check-item-mapping-coverage.mjs",
    "build": "npm run validate:data && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^7.2.4"
  }
}
